# Fase 4 — Panel Admin

**Fuente:** [spec §7 Routing + §8 Auth y roles](../specs/2026-04-21-migracion-laravel-design.md)
**Prerequisitos:** Fase 3 mergeada (ETL completo + 18 comandos + verify).
**Meta:** CRUDs admin para los 23 módulos + media manager + users admin, con autorización real (Policies + permisos Spatie), layout consistente, formularios validados y tests por módulo.

## Principios

- **Un pattern, muchas instancias.** Task 3 define el CRUD pattern completo (News) con controller + FormRequests + 3 vistas Blade + tests; los módulos siguientes lo aplican con variaciones chicas.
- **Thin controllers.** La lógica de negocio vive en Policies + FormRequests. Los controllers llaman `authorizeResource()` y delegan a repos/services solo cuando hay algo no-trivial.
- **Blade + Alpine + Tailwind.** Sin Livewire por ahora (se puede agregar después para el media manager). Formularios plain Blade con `@method('PUT')` y `@csrf`.
- **Form Requests por acción.** `StoreNewsRequest` + `UpdateNewsRequest`, no un único request acoplado.
- **Policies delegan a Spatie.** `return $user->can('news.view')` — una línea por action.
- **Tests siempre.** Un `AdminNewsTest` por módulo que valida: index autorizado, index denegado, create persiste, update modifica, delete elimina, policy bloquea a usuarios sin permiso.

## Estado inicial

Ya existe (de Fase 1):
- `routes/admin.php` con middleware `auth + role:admin|editor|moderator` y ruta dashboard.
- `app/Http/Controllers/Admin/DashboardController.php`
- `resources/views/admin/dashboard.blade.php`
- `resources/views/admin/layouts/admin.blade.php`
- `RolesAndPermissionsSeeder` con permisos `{module}.{action}` sembrados.
- Spatie `role` middleware alias registrado en `bootstrap/app.php`.
- 3 users demo con roles asignados.

Falta todo el resto del admin.

---

## Task 1: Admin Foundation

Layout y navegación compartidos, middleware ForcePasswordReset, flash messages, tabla reusable.

**Archivos:**
- Modify: `app/resources/views/admin/layouts/admin.blade.php` — layout completo
- Create: `app/resources/views/admin/partials/nav.blade.php` — sidebar con los 23 módulos
- Create: `app/resources/views/admin/partials/flash.blade.php` — mensajes de sesión
- Create: `app/resources/views/components/admin/` — componentes Blade (table, form-field, submit-button, breadcrumbs)
- Create: `app/app/Http/Middleware/ForcePasswordReset.php`
- Modify: `app/bootstrap/app.php` — registrar middleware `force.password.reset`
- Modify: `app/routes/admin.php` — aplicar middleware global + stub resources
- Modify: `app/app/Http/Controllers/Admin/DashboardController.php` — stats reales
- Create: `app/tests/Feature/Admin/AdminFoundationTest.php`

### Step 1: Middleware ForcePasswordReset

Crear `app/app/Http/Middleware/ForcePasswordReset.php`:

```php
<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;
use Symfony\Component\HttpFoundation\Response;

class ForcePasswordReset
{
    public function handle(Request $request, Closure $next): Response
    {
        $user = $request->user();

        if ($user && $user->must_reset_password) {
            // Permitir acceso a las rutas de reset + logout
            $allowed = ['password.edit', 'password.update', 'logout'];
            if (! in_array($request->route()?->getName(), $allowed, true)) {
                return redirect()
                    ->route('password.edit')
                    ->with('warning', 'Debés cambiar tu contraseña antes de continuar.');
            }
        }

        return $next($request);
    }
}
```

Registrar en `app/bootstrap/app.php` dentro de `withMiddleware`:

```php
->withMiddleware(function (Middleware $middleware) {
    // ... existentes (role alias, etc.)
    $middleware->alias([
        'role'                 => \Spatie\Permission\Middleware\RoleMiddleware::class,
        'permission'           => \Spatie\Permission\Middleware\PermissionMiddleware::class,
        'force.password.reset' => \App\Http\Middleware\ForcePasswordReset::class,
    ]);
})
```

### Step 2: Layout admin con sidebar

Reemplazar `app/resources/views/admin/layouts/admin.blade.php`:

```blade
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ $title ?? 'Admin' }} — Balneario El Cóndor</title>
    @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body class="bg-slate-100 text-slate-900">
<div class="min-h-screen flex">
    @include('admin.partials.nav')

    <div class="flex-1 flex flex-col">
        <header class="bg-white border-b border-slate-200 px-6 py-4 flex items-center justify-between">
            <div>
                @isset($breadcrumbs)
                    <x-admin.breadcrumbs :items="$breadcrumbs" />
                @endisset
                <h1 class="text-xl font-semibold">{{ $title ?? 'Admin' }}</h1>
            </div>
            <div class="flex items-center gap-3 text-sm">
                <span class="text-slate-600">{{ auth()->user()->name }}</span>
                <span class="text-xs bg-slate-200 rounded px-2 py-1">{{ auth()->user()->roles->pluck('name')->join(', ') }}</span>
                <form method="POST" action="{{ route('logout') }}">@csrf
                    <button class="text-red-600 hover:underline">Salir</button>
                </form>
            </div>
        </header>

        <main class="flex-1 p-6">
            @include('admin.partials.flash')
            {{ $slot ?? '' }}
            @yield('content')
        </main>
    </div>
</div>
</body>
</html>
```

### Step 3: Sidebar navigation

Crear `app/resources/views/admin/partials/nav.blade.php`:

```blade
@php
    $groups = [
        'Contenido' => [
            ['news.index', 'Noticias', 'news.view'],
            ['events.index', 'Eventos', 'events.view'],
            ['pages.index', 'Páginas', 'pages.view'],
            ['recipes.index', 'Recetas', 'recipes.view'],
        ],
        'Directorio' => [
            ['lodgings.index', 'Hospedajes', 'lodgings.view'],
            ['venues.index', 'Gourmet/Nocturnos', 'venues.view'],
            ['rentals.index', 'Alquileres', 'rentals.view'],
            ['service-providers.index', 'Servicios', 'service_providers.view'],
            ['nearby-places.index', 'Cercanos', 'nearby_places.view'],
            ['useful-info.index', 'Información útil', 'useful_info.view'],
            ['tides.index', 'Mareas', 'tides.view'],
        ],
        'Comunidad' => [
            ['classifieds.index', 'Clasificados', 'classifieds.view'],
            ['gallery.index', 'Galería', 'gallery.view'],
        ],
        'Engagement' => [
            ['surveys.index', 'Encuestas', 'surveys.view'],
            ['newsletter-subscribers.index', 'Suscriptores', 'newsletter_subscribers.view'],
            ['newsletter-campaigns.index', 'Campañas', 'newsletter_campaigns.view'],
            ['contact-messages.index', 'Mensajes', 'contact_messages.view'],
            ['advertising-contacts.index', 'Publicite', 'advertising_contacts.view'],
        ],
        'Sistema' => [
            ['users.index', 'Usuarios', 'users.view'],
        ],
    ];
    $user = auth()->user();
@endphp
<aside class="w-60 bg-slate-800 text-slate-100 p-4 space-y-5">
    <a href="{{ route('admin.dashboard') }}" class="block text-lg font-bold">
        Balneario Admin
    </a>
    @foreach($groups as $group => $items)
        <div>
            <p class="text-xs uppercase tracking-wide text-slate-400 mb-2">{{ $group }}</p>
            <ul class="space-y-1">
                @foreach($items as [$route, $label, $permission])
                    @can($permission)
                        <li>
                            <a href="{{ Route::has('admin.' . $route) ? route('admin.' . $route) : '#' }}"
                               class="block px-2 py-1 rounded hover:bg-slate-700 {{ request()->routeIs('admin.' . str_replace('.index', '', $route) . '.*') ? 'bg-slate-700' : '' }}">
                                {{ $label }}
                            </a>
                        </li>
                    @endcan
                @endforeach
            </ul>
        </div>
    @endforeach
</aside>
```

### Step 4: Flash messages partial

Crear `app/resources/views/admin/partials/flash.blade.php`:

```blade
@php $types = ['success' => 'green', 'warning' => 'amber', 'error' => 'red', 'info' => 'blue']; @endphp
@foreach($types as $type => $color)
    @if(session($type))
        <div class="mb-4 rounded border border-{{ $color }}-200 bg-{{ $color }}-50 text-{{ $color }}-800 px-4 py-3">
            {{ session($type) }}
        </div>
    @endif
@endforeach
@if($errors->any())
    <div class="mb-4 rounded border border-red-200 bg-red-50 text-red-800 px-4 py-3">
        <p class="font-medium">Errores:</p>
        <ul class="list-disc pl-5 text-sm mt-1">
            @foreach($errors->all() as $e) <li>{{ $e }}</li> @endforeach
        </ul>
    </div>
@endif
```

### Step 5: Componentes Blade reutilizables

Crear `app/resources/views/components/admin/breadcrumbs.blade.php`:

```blade
@props(['items' => []])
<nav class="text-xs text-slate-500 mb-1">
    @foreach($items as $label => $url)
        @if(!$loop->last)
            <a href="{{ $url }}" class="hover:underline">{{ $label }}</a> /
        @else
            <span class="text-slate-700">{{ is_int($label) ? $url : $label }}</span>
        @endif
    @endforeach
</nav>
```

Crear `app/resources/views/components/admin/form-field.blade.php`:

```blade
@props(['name', 'label', 'type' => 'text', 'value' => null, 'required' => false, 'help' => null])
<div class="mb-4">
    <label for="{{ $name }}" class="block text-sm font-medium text-slate-700 mb-1">
        {{ $label }}@if($required) <span class="text-red-500">*</span>@endif
    </label>
    @if($type === 'textarea')
        <textarea id="{{ $name }}" name="{{ $name }}" rows="4"
                  {{ $attributes->merge(['class' => 'w-full border-slate-300 rounded']) }}>{{ old($name, $value) }}</textarea>
    @elseif($type === 'select')
        <select id="{{ $name }}" name="{{ $name }}"
                {{ $attributes->merge(['class' => 'w-full border-slate-300 rounded']) }}>
            {{ $slot }}
        </select>
    @elseif($type === 'checkbox')
        <input id="{{ $name }}" name="{{ $name }}" type="checkbox" value="1"
               {{ old($name, $value) ? 'checked' : '' }}
               class="rounded border-slate-300">
    @else
        <input id="{{ $name }}" name="{{ $name }}" type="{{ $type }}"
               value="{{ old($name, $value) }}"
               {{ $attributes->merge(['class' => 'w-full border-slate-300 rounded']) }}>
    @endif
    @if($help)<p class="text-xs text-slate-500 mt-1">{{ $help }}</p>@endif
    @error($name)<p class="text-xs text-red-600 mt-1">{{ $message }}</p>@enderror
</div>
```

Crear `app/resources/views/components/admin/submit-button.blade.php`:

```blade
@props(['label' => 'Guardar'])
<button type="submit"
        {{ $attributes->merge(['class' => 'bg-slate-800 text-white rounded px-4 py-2 hover:bg-slate-700']) }}>
    {{ $label }}
</button>
```

### Step 6: Dashboard con stats

Reemplazar `app/app/Http/Controllers/Admin/DashboardController.php`:

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Models\{
    Classified, ContactMessage, Event, Lodging,
    News, NewsletterSubscriber, SurveyResponse,
};
use Illuminate\View\View;

class DashboardController extends Controller
{
    public function __invoke(): View
    {
        return view('admin.dashboard', [
            'stats' => [
                'News'          => News::count(),
                'Eventos'       => Event::count(),
                'Hospedajes'    => Lodging::count(),
                'Clasificados'  => Classified::count(),
                'Mensajes'      => ContactMessage::where('read', false)->count(),
                'Subscribers'   => NewsletterSubscriber::where('status', 'confirmed')->count(),
                'Responses'    => SurveyResponse::count(),
            ],
        ]);
    }
}
```

Y dashboard view:

```blade
<x-admin.layouts.admin title="Dashboard">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        @foreach($stats as $label => $count)
            <div class="bg-white rounded shadow p-4">
                <p class="text-xs uppercase text-slate-500">{{ $label }}</p>
                <p class="text-2xl font-bold mt-1">{{ $count }}</p>
            </div>
        @endforeach
    </div>
</x-admin.layouts.admin>
```

### Step 7: Route stubs para los 23 módulos

Reemplazar `app/routes/admin.php`:

```php
<?php

use App\Http\Controllers\Admin\{
    AdvertisingContactController, ClassifiedController, ClassifiedContactController,
    ContactMessageController, DashboardController, EventController,
    EventRegistrationController, GalleryImageController, LodgingController,
    NearbyPlaceController, NewsCategoryController, NewsController,
    NewsletterCampaignController, NewsletterSubscriberController, PageController,
    RecipeController, RentalController, ServiceProviderController, SurveyController,
    SurveyResponseController, TideController, UsefulInfoController, UserController,
    VenueController,
};
use Illuminate\Support\Facades\Route;

Route::middleware(['auth', 'force.password.reset', 'role:admin|editor|moderator'])
    ->prefix('admin')
    ->name('admin.')
    ->group(function () {
        Route::get('/', DashboardController::class)->name('dashboard');

        // Contenido
        Route::resource('news', NewsController::class);
        Route::resource('news-categories', NewsCategoryController::class);
        Route::resource('events', EventController::class);
        Route::resource('events.registrations', EventRegistrationController::class)
            ->shallow()->only(['index', 'show', 'destroy']);
        Route::resource('pages', PageController::class);
        Route::resource('recipes', RecipeController::class);

        // Directorio
        Route::resource('lodgings', LodgingController::class);
        Route::resource('venues', VenueController::class);
        Route::resource('rentals', RentalController::class);
        Route::resource('service-providers', ServiceProviderController::class)
            ->parameters(['service-providers' => 'serviceProvider']);
        Route::resource('nearby-places', NearbyPlaceController::class)
            ->parameters(['nearby-places' => 'nearbyPlace']);
        Route::resource('useful-info', UsefulInfoController::class)
            ->parameters(['useful-info' => 'usefulInfo']);
        Route::resource('tides', TideController::class);

        // Comunidad
        Route::resource('classifieds', ClassifiedController::class);
        Route::resource('classifieds.contacts', ClassifiedContactController::class)
            ->shallow()->only(['index', 'show', 'destroy']);
        Route::resource('gallery', GalleryImageController::class)
            ->parameters(['gallery' => 'galleryImage']);

        // Engagement
        Route::resource('surveys', SurveyController::class);
        Route::resource('surveys.responses', SurveyResponseController::class)
            ->shallow()->only(['index', 'show', 'destroy']);
        Route::resource('newsletter-subscribers', NewsletterSubscriberController::class)
            ->parameters(['newsletter-subscribers' => 'subscriber'])
            ->except(['show']);
        Route::resource('newsletter-campaigns', NewsletterCampaignController::class)
            ->parameters(['newsletter-campaigns' => 'campaign']);
        Route::resource('contact-messages', ContactMessageController::class)
            ->parameters(['contact-messages' => 'message'])
            ->only(['index', 'show', 'destroy']);
        Route::resource('advertising-contacts', AdvertisingContactController::class)
            ->parameters(['advertising-contacts' => 'adContact'])
            ->only(['index', 'show', 'destroy']);

        // Sistema
        Route::resource('users', UserController::class)->middleware('role:admin');
    });
```

**Nota:** estos controllers todavía no existen — se crean en Tasks 3-9. Pero registrarlos desde ahora permite que el sidebar use `route()` helper con `Route::has()` sin romper.

### Step 8: Test foundation

Crear `app/tests/Feature/Admin/AdminFoundationTest.php`:

```php
<?php

namespace Tests\Feature\Admin;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class AdminFoundationTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        $this->seed(\Database\Seeders\RolesAndPermissionsSeeder::class);
    }

    public function test_unauthenticated_user_cannot_access_admin(): void
    {
        $this->get('/admin')->assertRedirect('/login');
    }

    public function test_authenticated_user_without_role_is_forbidden(): void
    {
        $user = User::factory()->create();
        $this->actingAs($user)->get('/admin')->assertForbidden();
    }

    public function test_admin_user_sees_dashboard(): void
    {
        $user = User::factory()->create()->assignRole('admin');
        $response = $this->actingAs($user)->get('/admin');
        $response->assertOk()->assertSee('Dashboard');
    }

    public function test_user_with_must_reset_password_is_redirected(): void
    {
        $user = User::factory()->create(['must_reset_password' => true]);
        $user->assignRole('admin');
        $this->actingAs($user)->get('/admin')
            ->assertRedirect(route('password.edit'));
    }

    public function test_dashboard_shows_counts(): void
    {
        $user = User::factory()->create()->assignRole('admin');
        \App\Models\News::factory()->count(3)->create();
        $response = $this->actingAs($user)->get('/admin');
        $response->assertSee('News')->assertSee('3');
    }
}
```

### Step 9: Verificar

```bash
cd /home/juan/sitios/balneario-el-condor/app
docker compose exec -T app php artisan test tests/Feature/Admin/
docker compose exec -T app php artisan test  # suite completa verde
```

### Step 10: Commit

```bash
git add app/
git commit -m "feat(admin): foundation (layout, sidebar, middleware force-password-reset, dashboard)"
```

---

## Task 2: Policies base + helper de tests

Crear las 24 Policies delegando a `$user->can('{module}.{action}')` y un `AdminTestCase` base que simplifica los tests por módulo.

**Archivos:**
- Create: `app/app/Policies/*Policy.php` (24 archivos siguiendo el mismo pattern)
- Create: `app/app/Providers/AuthServiceProvider.php` (si no existe) o modify: `AppServiceProvider` para registrar las policies
- Create: `app/tests/Feature/Admin/AdminTestCase.php`

### Step 1: Pattern de una Policy

Crear `app/app/Policies/NewsPolicy.php` (referencia):

```php
<?php

namespace App\Policies;

use App\Models\News;
use App\Models\User;

class NewsPolicy
{
    public function viewAny(User $user): bool   { return $user->can('news.view'); }
    public function view(User $user, News $n): bool    { return $user->can('news.view'); }
    public function create(User $user): bool           { return $user->can('news.create'); }
    public function update(User $user, News $n): bool  { return $user->can('news.update'); }
    public function delete(User $user, News $n): bool  { return $user->can('news.delete'); }
    public function restore(User $user, News $n): bool { return $user->can('news.update'); }
}
```

### Step 2: Generar las 23 policies restantes

Mapping modelo → permission prefix → archivo policy:

| Modelo | Prefix | Policy class |
|---|---|---|
| News | `news` | `NewsPolicy` |
| NewsCategory | `news_categories` | `NewsCategoryPolicy` |
| Event | `events` | `EventPolicy` |
| EventRegistration | `event_registrations` | `EventRegistrationPolicy` |
| Lodging | `lodgings` | `LodgingPolicy` |
| Venue | `venues` | `VenuePolicy` |
| Rental | `rentals` | `RentalPolicy` |
| Classified | `classifieds` | `ClassifiedPolicy` |
| ClassifiedContact | `classified_contacts` | `ClassifiedContactPolicy` |
| ServiceProvider | `service_providers` | `ServiceProviderPolicy` |
| Recipe | `recipes` | `RecipePolicy` |
| Tide | `tides` | `TidePolicy` |
| GalleryImage | `gallery` | `GalleryImagePolicy` |
| NearbyPlace | `nearby_places` | `NearbyPlacePolicy` |
| UsefulInfo | `useful_info` | `UsefulInfoPolicy` |
| Page | `pages` | `PagePolicy` |
| Survey | `surveys` | `SurveyPolicy` |
| SurveyResponse | `survey_responses` | `SurveyResponsePolicy` |
| NewsletterSubscriber | `newsletter_subscribers` | `NewsletterSubscriberPolicy` |
| NewsletterCampaign | `newsletter_campaigns` | `NewsletterCampaignPolicy` |
| ContactMessage | `contact_messages` | `ContactMessagePolicy` |
| AdvertisingContact | `advertising_contacts` | `AdvertisingContactPolicy` |
| User | `users` | `UserPolicy` |

Todas siguen el mismo pattern de NewsPolicy. Laravel 11 autodiscovery de policies funciona si están en `App\Policies\{Model}Policy` y el modelo en `App\Models\{Model}`.

El implementador puede generar los 23 restantes con un script pequeño o un loop en PHP. Ejemplo (ejecutar desde el contenedor):

```bash
docker compose exec -T app bash -c 'cd app/Policies && for m in NewsCategory Event EventRegistration Lodging Venue Rental Classified ClassifiedContact ServiceProvider Recipe Tide GalleryImage NearbyPlace UsefulInfo Page Survey SurveyResponse NewsletterSubscriber NewsletterCampaign ContactMessage AdvertisingContact User; do
  prefix=$(echo $m | sed -E "s/([A-Z])/_\L\1/g" | sed "s/^_//")
  # mapping manual de plurales especiales:
  # NewsCategory → news_categories, Event → events, etc.
  echo "pendiente $m ($prefix)"
done'
```

En vez del bash, copiar/pegar los 23 archivos con el prefix correcto del mapping arriba.

### Step 3: Verificar autodiscovery

Laravel 11 descubre las policies si `App\Providers\AuthServiceProvider` no existe (es opcional). Si hay descubrimiento manual, agregar en `AppServiceProvider::boot`:

```php
use Illuminate\Support\Facades\Gate;
Gate::policy(\App\Models\News::class, \App\Policies\NewsPolicy::class);
// ... (23 más)
```

Preferir autodiscovery (Laravel 11 lo hace por convención).

### Step 4: AdminTestCase base

Crear `app/tests/Feature/Admin/AdminTestCase.php`:

```php
<?php

namespace Tests\Feature\Admin;

use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

abstract class AdminTestCase extends TestCase
{
    use RefreshDatabase;

    protected User $admin;
    protected User $editor;
    protected User $moderator;

    protected function setUp(): void
    {
        parent::setUp();
        $this->seed(\Database\Seeders\RolesAndPermissionsSeeder::class);
        $this->admin     = User::factory()->create()->assignRole('admin');
        $this->editor    = User::factory()->create()->assignRole('editor');
        $this->moderator = User::factory()->create()->assignRole('moderator');
    }

    protected function asAdmin(): self     { $this->actingAs($this->admin); return $this; }
    protected function asEditor(): self    { $this->actingAs($this->editor); return $this; }
    protected function asModerator(): self { $this->actingAs($this->moderator); return $this; }
    protected function asGuest(User $u): self { $this->actingAs(User::factory()->create()); return $this; }
}
```

### Step 5: Test policies smoke

Crear `app/tests/Feature/Admin/PoliciesTest.php`:

```php
<?php

namespace Tests\Feature\Admin;

use App\Models\News;

class PoliciesTest extends AdminTestCase
{
    public function test_admin_can_manage_news(): void
    {
        $news = News::factory()->create();
        $this->assertTrue($this->admin->can('viewAny', News::class));
        $this->assertTrue($this->admin->can('update', $news));
        $this->assertTrue($this->admin->can('delete', $news));
    }

    public function test_editor_can_manage_news(): void
    {
        $news = News::factory()->create();
        $this->assertTrue($this->editor->can('viewAny', News::class));
        $this->assertTrue($this->editor->can('update', $news));
    }

    public function test_moderator_cannot_manage_news(): void
    {
        $this->assertFalse($this->moderator->can('viewAny', News::class));
        $this->assertFalse($this->moderator->can('create', News::class));
    }

    public function test_moderator_can_view_and_delete_moderable(): void
    {
        $classified = \App\Models\Classified::factory()->create();
        $this->assertTrue($this->moderator->can('viewAny', \App\Models\Classified::class));
        $this->assertTrue($this->moderator->can('delete', $classified));
        $this->assertFalse($this->moderator->can('update', $classified));
    }
}
```

### Step 6: Commit

```bash
git add app/
git commit -m "feat(admin): 23 Policies + AdminTestCase base + tests smoke de autorización"
```

---

## Task 3: CRUD pattern de referencia — News

Implementación completa del CRUD de News como plantilla para los demás módulos. Incluye: controller, 2 form requests, 3 vistas Blade (index, create, edit), y test completo.

**Archivos:**
- Create: `app/app/Http/Controllers/Admin/NewsController.php`
- Create: `app/app/Http/Requests/Admin/StoreNewsRequest.php`
- Create: `app/app/Http/Requests/Admin/UpdateNewsRequest.php`
- Create: `app/resources/views/admin/news/index.blade.php`
- Create: `app/resources/views/admin/news/create.blade.php`
- Create: `app/resources/views/admin/news/edit.blade.php`
- Create: `app/resources/views/admin/news/_form.blade.php` (parcial compartido)
- Create: `app/tests/Feature/Admin/AdminNewsTest.php`

### Step 1: Controller

Crear `app/app/Http/Controllers/Admin/NewsController.php`:

```php
<?php

namespace App\Http\Controllers\Admin;

use App\Http\Controllers\Controller;
use App\Http\Requests\Admin\StoreNewsRequest;
use App\Http\Requests\Admin\UpdateNewsRequest;
use App\Models\News;
use App\Models\NewsCategory;
use Illuminate\Http\RedirectResponse;
use Illuminate\Support\Str;
use Illuminate\View\View;

class NewsController extends Controller
{
    public function __construct()
    {
        $this->authorizeResource(News::class, 'news');
    }

    public function index(): View
    {
        $news = News::query()
            ->with('category')
            ->latest('published_at')
            ->paginate(20);

        return view('admin.news.index', compact('news'));
    }

    public function create(): View
    {
        return view('admin.news.create', [
            'news'       => new News(),
            'categories' => NewsCategory::orderBy('name')->get(),
        ]);
    }

    public function store(StoreNewsRequest $request): RedirectResponse
    {
        $data = $request->validated();
        $data['slug'] = $data['slug'] ?: Str::slug($data['title']);
        $news = News::create($data);

        return redirect()
            ->route('admin.news.edit', $news)
            ->with('success', 'Noticia creada.');
    }

    public function edit(News $news): View
    {
        return view('admin.news.edit', [
            'news'       => $news,
            'categories' => NewsCategory::orderBy('name')->get(),
        ]);
    }

    public function update(UpdateNewsRequest $request, News $news): RedirectResponse
    {
        $data = $request->validated();
        if (empty($data['slug'])) {
            $data['slug'] = Str::slug($data['title']);
        }
        $news->update($data);

        return redirect()
            ->route('admin.news.edit', $news)
            ->with('success', 'Noticia actualizada.');
    }

    public function destroy(News $news): RedirectResponse
    {
        $news->delete();
        return redirect()
            ->route('admin.news.index')
            ->with('success', 'Noticia eliminada.');
    }
}
```

### Step 2: FormRequests

Crear `app/app/Http/Requests/Admin/StoreNewsRequest.php`:

```php
<?php

namespace App\Http\Requests\Admin;

use Illuminate\Foundation\Http\FormRequest;

class StoreNewsRequest extends FormRequest
{
    public function authorize(): bool
    {
        return $this->user()?->can('news.create') ?? false;
    }

    public function rules(): array
    {
        return [
            'title'            => ['required', 'string', 'max:300'],
            'slug'             => ['nullable', 'string', 'max:255', 'unique:news,slug'],
            'excerpt'          => ['nullable', 'string', 'max:500'],
            'body'             => ['required', 'string'],
            'news_category_id' => ['nullable', 'integer', 'exists:news_categories,id'],
            'published_at'     => ['nullable', 'date'],
            'featured'         => ['nullable', 'boolean'],
            'views'            => ['nullable', 'integer', 'min:0'],
        ];
    }

    public function prepareForValidation(): void
    {
        $this->merge([
            'featured' => $this->boolean('featured'),
        ]);
    }
}
```

Crear `app/app/Http/Requests/Admin/UpdateNewsRequest.php`:

```php
<?php

namespace App\Http\Requests\Admin;

use Illuminate\Foundation\Http\FormRequest;
use Illuminate\Validation\Rule;

class UpdateNewsRequest extends FormRequest
{
    public function authorize(): bool
    {
        return $this->user()?->can('news.update') ?? false;
    }

    public function rules(): array
    {
        $news = $this->route('news');
        return [
            'title'            => ['required', 'string', 'max:300'],
            'slug'             => ['nullable', 'string', 'max:255', Rule::unique('news', 'slug')->ignore($news)],
            'excerpt'          => ['nullable', 'string', 'max:500'],
            'body'             => ['required', 'string'],
            'news_category_id' => ['nullable', 'integer', 'exists:news_categories,id'],
            'published_at'     => ['nullable', 'date'],
            'featured'         => ['nullable', 'boolean'],
            'views'            => ['nullable', 'integer', 'min:0'],
        ];
    }

    public function prepareForValidation(): void
    {
        $this->merge(['featured' => $this->boolean('featured')]);
    }
}
```

### Step 3: Vistas Blade

Crear `app/resources/views/admin/news/index.blade.php`:

```blade
<x-admin.layouts.admin title="Noticias"
    :breadcrumbs="[
        'Admin' => route('admin.dashboard'),
        'Noticias' => null,
    ]">
    <div class="flex justify-end mb-4">
        @can('news.create')
            <a href="{{ route('admin.news.create') }}"
               class="bg-slate-800 text-white rounded px-4 py-2 hover:bg-slate-700">
                Nueva noticia
            </a>
        @endcan
    </div>

    <div class="bg-white rounded shadow overflow-x-auto">
        <table class="min-w-full text-sm">
            <thead class="bg-slate-50 text-slate-600 uppercase text-xs">
                <tr>
                    <th class="text-left p-3">Título</th>
                    <th class="text-left p-3">Categoría</th>
                    <th class="text-left p-3">Publicación</th>
                    <th class="text-right p-3">Vistas</th>
                    <th class="text-right p-3"></th>
                </tr>
            </thead>
            <tbody class="divide-y divide-slate-100">
                @forelse($news as $item)
                    <tr>
                        <td class="p-3">
                            <a href="{{ route('admin.news.edit', $item) }}" class="hover:underline">
                                {{ $item->title }}
                            </a>
                        </td>
                        <td class="p-3 text-slate-600">{{ $item->category?->name ?? '—' }}</td>
                        <td class="p-3 text-slate-600">{{ $item->published_at?->format('d/m/Y') ?? '—' }}</td>
                        <td class="p-3 text-right text-slate-600">{{ $item->views }}</td>
                        <td class="p-3 text-right">
                            @can('news.delete')
                                <form method="POST" action="{{ route('admin.news.destroy', $item) }}"
                                      onsubmit="return confirm('¿Eliminar noticia?')" class="inline">
                                    @csrf @method('DELETE')
                                    <button class="text-red-600 hover:underline">Eliminar</button>
                                </form>
                            @endcan
                        </td>
                    </tr>
                @empty
                    <tr><td colspan="5" class="p-6 text-center text-slate-500">Sin noticias.</td></tr>
                @endforelse
            </tbody>
        </table>
    </div>

    <div class="mt-4">{{ $news->links() }}</div>
</x-admin.layouts.admin>
```

Crear `app/resources/views/admin/news/create.blade.php`:

```blade
<x-admin.layouts.admin title="Nueva noticia"
    :breadcrumbs="[
        'Admin' => route('admin.dashboard'),
        'Noticias' => route('admin.news.index'),
        'Nueva' => null,
    ]">
    <form method="POST" action="{{ route('admin.news.store') }}" class="bg-white rounded shadow p-6 max-w-3xl">
        @csrf
        @include('admin.news._form')
        <x-admin.submit-button label="Crear" />
    </form>
</x-admin.layouts.admin>
```

Crear `app/resources/views/admin/news/edit.blade.php`:

```blade
<x-admin.layouts.admin title="Editar: {{ $news->title }}"
    :breadcrumbs="[
        'Admin' => route('admin.dashboard'),
        'Noticias' => route('admin.news.index'),
        'Editar' => null,
    ]">
    <form method="POST" action="{{ route('admin.news.update', $news) }}" class="bg-white rounded shadow p-6 max-w-3xl">
        @csrf @method('PUT')
        @include('admin.news._form')
        <x-admin.submit-button label="Actualizar" />
    </form>
</x-admin.layouts.admin>
```

Crear `app/resources/views/admin/news/_form.blade.php`:

```blade
<x-admin.form-field name="title" label="Título" :value="$news->title" required />
<x-admin.form-field name="slug" label="Slug (auto si vacío)" :value="$news->slug" help="URL amigable. Se genera desde el título si no se completa." />
<x-admin.form-field name="excerpt" label="Resumen" type="textarea" :value="$news->excerpt" />
<x-admin.form-field name="body" label="Cuerpo" type="textarea" :value="$news->body" required class="min-h-[200px]" />

<x-admin.form-field name="news_category_id" label="Categoría" type="select">
    <option value="">— Sin categoría —</option>
    @foreach($categories as $cat)
        <option value="{{ $cat->id }}" @selected(old('news_category_id', $news->news_category_id) == $cat->id)>{{ $cat->name }}</option>
    @endforeach
</x-admin.form-field>

<x-admin.form-field name="published_at" label="Fecha de publicación" type="datetime-local"
    :value="optional($news->published_at)->format('Y-m-d\TH:i')" />

<div class="flex items-center gap-2 mb-4">
    <x-admin.form-field name="featured" label="" type="checkbox" :value="$news->featured" />
    <label for="featured" class="text-sm">Destacada</label>
</div>
```

### Step 4: Tests por módulo (pattern)

Crear `app/tests/Feature/Admin/AdminNewsTest.php`:

```php
<?php

namespace Tests\Feature\Admin;

use App\Models\News;
use App\Models\NewsCategory;

class AdminNewsTest extends AdminTestCase
{
    public function test_admin_sees_index(): void
    {
        News::factory()->count(3)->create();
        $this->asAdmin()->get('/admin/news')->assertOk()->assertSee('Noticias');
    }

    public function test_moderator_cannot_see_index(): void
    {
        $this->asModerator()->get('/admin/news')->assertForbidden();
    }

    public function test_editor_can_create(): void
    {
        $cat = NewsCategory::factory()->create();
        $response = $this->asEditor()->post('/admin/news', [
            'title' => 'Nueva noticia',
            'body'  => 'Cuerpo largo.',
            'news_category_id' => $cat->id,
            'published_at' => now()->format('Y-m-d\TH:i'),
        ]);
        $response->assertRedirect();
        $this->assertDatabaseHas('news', ['title' => 'Nueva noticia']);
    }

    public function test_store_validates_required(): void
    {
        $this->asEditor()->post('/admin/news', [])
            ->assertSessionHasErrors(['title', 'body']);
    }

    public function test_admin_can_update(): void
    {
        $news = News::factory()->create();
        $this->asAdmin()->put("/admin/news/{$news->id}", [
            'title' => 'Nuevo título',
            'body'  => $news->body,
        ])->assertRedirect();
        $this->assertDatabaseHas('news', ['id' => $news->id, 'title' => 'Nuevo título']);
    }

    public function test_admin_can_delete(): void
    {
        $news = News::factory()->create();
        $this->asAdmin()->delete("/admin/news/{$news->id}")->assertRedirect();
        $this->assertSoftDeleted('news', ['id' => $news->id]);
    }

    public function test_moderator_cannot_delete(): void
    {
        $news = News::factory()->create();
        $this->asModerator()->delete("/admin/news/{$news->id}")->assertForbidden();
    }
}
```

### Step 5: Verificar

```bash
docker compose exec -T app php artisan test tests/Feature/Admin/AdminNewsTest.php
docker compose exec -T app php artisan test  # suite completa
```

### Step 6: Commit

```bash
git add app/
git commit -m "feat(admin): CRUD de News (pattern de referencia para los demás módulos)"
```

---

## Task 4: Contenido editorial — Events + EventRegistrations + Pages + Recipes

Aplicar pattern de Task 3 a cuatro módulos. Cada uno necesita Controller + 2 FormRequests + 3 vistas Blade + Test.

### Events

- **Fields:** title, slug, description, location, starts_at, ends_at, all_day, featured, accepts_registrations, external_url, sort_order, news_category_id → no (events no tiene category en el schema).
- **Index:** filtro por "featured" y "próximos".
- **Edit:** si `accepts_registrations=true`, mostrar link "Ver inscripciones" → `admin.events.registrations.index`.
- **ValidateFechas:** `starts_at` opcional (datetime), `ends_at >= starts_at` cuando ambos.

### EventRegistrations

- Solo index + show + destroy (no create/edit en admin — se crean desde el público).
- Index filtra por `event_id`.
- Show: muestra todos los campos + `extra_data` JSON formateado.
- Destroy: soft delete del registration.

### Pages

- **Fields:** slug, title, content (longText rich text), meta_description, published.
- **Edit:** usar Trix/TinyMCE o textarea simple (plan = textarea; integrar editor en Task 9).
- **Slug:** siempre manual (no autogenerado) porque las páginas tienen URLs estables.
- **Published:** toggle boolean.

### Recipes

- **Fields:** title, slug, prep_minutes, cook_minutes, servings, cost, ingredients, instructions, author, published_on.
- **Ingredientes:** textarea que acepta markdown (una línea por ingrediente).
- **Slug:** auto desde title.

### Estructura

Para cada módulo, el implementador debe:
1. Crear `app/Http/Controllers/Admin/{Module}Controller.php` siguiendo el pattern de `NewsController`.
2. Crear `app/Http/Requests/Admin/Store{Module}Request.php` + `Update{Module}Request.php`.
3. Crear `resources/views/admin/{module}/{index,create,edit,_form}.blade.php`.
4. Crear `tests/Feature/Admin/Admin{Module}Test.php` siguiendo el pattern de `AdminNewsTest`.

### Verificación global

```bash
docker compose exec -T app php artisan test tests/Feature/Admin/
```

Esperado: los 5 tests nuevos (AdminEventTest, AdminEventRegistrationTest, AdminPageTest, AdminRecipeTest + los previos) todos verdes.

### Commit

```bash
git add app/
git commit -m "feat(admin): CRUDs de events + event-registrations + pages + recipes"
```

---

## Task 5: Directorio — Lodgings + Venues + Rentals + ServiceProviders + NearbyPlaces + UsefulInfo + Tides

Siete módulos más. Todos con campos de contacto y coordenadas; la mayoría tiene media asociada.

### Lodgings

- **Fields:** name, slug, type (hotel|casa|camping|hostel|other), description, address, phone, email, website, latitude, longitude, views, featured, sort_order.
- **Index:** columna `type` + filtro por tipo.
- **Edit:** mapa Leaflet para seleccionar coordenadas (implementar simple: 2 inputs lat/lng + link "Abrir en Google Maps" para copiar).

### Venues

- **Fields:** name, slug, category (gourmet|nightlife), description, address, phone, latitude, longitude, views.
- **Index:** filtro por category.

### Rentals

- **Fields:** title, slug, places (int), phone, email, address, contact_name, description.
- Sin coordenadas.

### ServiceProviders

- **Fields:** name, slug, description, contact_name, contact_email, phone, address, latitude, longitude.

### NearbyPlaces

- **Fields:** title, slug, description, address, latitude, longitude, views.

### UsefulInfo

- **Fields:** title, phone, website, email, address, latitude, longitude, sort_order.
- Sin slug (no se ruteable en el público).

### Tides

- **Fields:** location, date, first_high, first_high_height, first_low, first_low_height, second_high, second_high_height, second_low, second_low_height.
- **Index:** ordenado por date desc, con mes/año selector.
- **Import CSV:** agregar botón "Importar CSV" que sube un archivo con columnas `date,first_high,first_high_height,first_low,first_low_height,second_high,second_high_height,second_low,second_low_height` y hace `Tide::updateOrCreate(['location', 'date'], [...])`.
- **Generar migración del importer:** crear `app/Http/Controllers/Admin/TideImportController.php` con acción `import` (POST `/admin/tides/import`), FormRequest con `file:csv|txt`.

### Estructura

Mismo pattern que Task 4 — un Controller + FormRequests + vistas + tests por módulo.

### Verificación

```bash
docker compose exec -T app php artisan test tests/Feature/Admin/
```

### Commit

```bash
git add app/
git commit -m "feat(admin): CRUDs de lodgings + venues + rentals + service-providers + nearby-places + useful-info + tides (+ import CSV)"
```

---

## Task 6: Comunidad — Classifieds + ClassifiedContacts + Gallery

### Classifieds

- **Fields:** title, slug, description, classified_category_id, contact_name, contact_email, contact_phone, address, latitude, longitude, video_url, views, published_at.
- **Index:** filtro por categoría + estado (destacado/activo).
- **Moderation:** moderator SOLO ve index + delete (no create/edit).

### ClassifiedContacts

- **Shallow resource:** `admin/classifieds/{classified}/contacts` index + show + destroy.
- Solo lectura. Muestra mensajes enviados al dueño del clasificado.

### Gallery

- **Fields:** title, slug, description, path, thumb_path, original_path, taken_on, views.
- **Upload:** formulario que sube archivo → `storage/app/public/gallery/` + genera thumb (120x120) con GD; guarda los 3 paths.
- **Tooling:** usar `intervention/image` si está, sino GD nativo.

### Estructura

Mismo pattern. Tests por módulo.

### Commit

```bash
git add app/
git commit -m "feat(admin): CRUDs de classifieds + classified-contacts + gallery (upload + thumb)"
```

---

## Task 7: Engagement — Surveys + SurveyResponses + NewsletterSubscribers + NewsletterCampaigns + ContactMessages + AdvertisingContacts

### Surveys

- **Fields:** title, question, options (JSON array), active.
- **Edit:** repeater para editar options (key + label). Usar Alpine `x-data` con un array y botones +/-.

### SurveyResponses

- Shallow resource: `admin/surveys/{survey}/responses` index + show + destroy.
- Index con gráfico simple (bar chart Alpine) de distribución por option_key.

### NewsletterSubscribers

- **Index + destroy**. No create/edit (manejado por suscripción pública).
- Filtro por status (pending, confirmed, unsubscribed).
- Action "Exportar CSV" → descarga `subscribers-YYYY-MM-DD.csv`.

### NewsletterCampaigns

- **CRUD completo.**
- **Fields:** subject, body_html, body_text, scheduled_at, status.
- **Action "Enviar ahora"** → POST `/admin/newsletter-campaigns/{campaign}/send` → despacha `SendCampaign` job.
- **Job** `App\Jobs\SendCampaign` lee subscribers confirmados, chunks de 100, manda via Resend. Por ahora mock (log al channel 'mail'), integración Resend en Plan 6.

### ContactMessages

- **Only index + show + destroy.**
- Botón "Marcar como leído" → PUT a `messages/{message}/mark-read` toggle del campo `read`.

### AdvertisingContacts

- **Only index + show + destroy.**

### Commit

```bash
git add app/
git commit -m "feat(admin): CRUDs de engagement (surveys, newsletter, contact, advertising) + SendCampaign job stub"
```

---

## Task 8: Users + Roles (solo admin)

### UserController

- Middleware restrict: `role:admin`.
- **Index:** lista users + sus roles.
- **Create/Update:** nombre, email, password (nullable en update), rol (single choice entre admin/editor/moderator).
- **Destroy:** no permite borrar al propio usuario logueado.

### Estructura

- `app/Http/Controllers/Admin/UserController.php`
- `app/Http/Requests/Admin/StoreUserRequest.php` + `UpdateUserRequest.php`
  - Reglas: email unique (con ignore en update), password required en create, confirmed.
- Vistas `admin/users/{index,create,edit,_form}.blade.php`.
- Test `AdminUserTest`: admin can manage, editor cannot access.

### Commit

```bash
git add app/
git commit -m "feat(admin): CRUD de users con gestión de roles Spatie (solo admin)"
```

---

## Task 9: Media Manager

Subida, listado, reorder y eliminación de imágenes vinculadas a modelos polimórficos (News, Lodging, Venue, Rental, Classified, Recipe, ServiceProvider, Event, Page, NearbyPlace).

### Archivos

- Create: `app/app/Http/Controllers/Admin/MediaController.php` con acciones:
  - `POST /admin/media` — upload (requiere `mediable_type` + `mediable_id`)
  - `DELETE /admin/media/{media}` — eliminar
  - `PATCH /admin/media/reorder` — body: `{ items: [{id, sort_order}, ...] }`
- Create: `app/app/Http/Requests/Admin/StoreMediaRequest.php`
  - Rules: `file|required|image|max:5120`, `mediable_type|required`, `mediable_id|required|integer`, `alt|nullable|string`.
- Create: `app/resources/views/components/admin/media-manager.blade.php`
  - Component Blade que recibe `:mediable` (un modelo) y renderiza grid de Media actual + form upload + JS para drag-reorder con SortableJS.
- Create: `app/resources/js/media-manager.js` — Alpine component con integración SortableJS.
- Install: `npm install sortablejs` en el contenedor.
- Upload: guarda en `storage/app/public/media/YYYY/MM/` con nombre `{ulid}.jpg`.
- Usa `intervention/image` para resize (crear versión de 1200px máximo de ancho).

### Integración

En los blade `_form.blade.php` de modelos con media, agregar:

```blade
@if($model->exists)
    <x-admin.media-manager :mediable="$model" class="mt-6" />
@endif
```

### Test

- `app/tests/Feature/Admin/AdminMediaTest.php`:
  - upload válido genera Media + archivo en disco
  - upload de no-imagen falla validación
  - delete quita Media + archivo
  - reorder actualiza sort_order

### Commit

```bash
git add app/
git commit -m "feat(admin): media manager con upload + reorder drag&drop + delete"
```

---

## Task 10: Tests E2E + refinamiento

### Paginación

Revisar que todos los CRUDs usen `paginate(20)` en index.

### Búsqueda

Agregar input search simple en los módulos con muchas filas (news, classifieds, gallery, tides, newsletter_subscribers). Query param `?q=...`.

### Feature tests por rol

Crear `app/tests/Feature/Admin/AdminRolesMatrixTest.php` que itera por todos los módulos verificando:

```php
test('editor puede acceder a {module}.index excepto users', function() {...});
test('moderator solo ve moderable modules', function() {...});
```

### Smoke E2E

Crear `app/tests/Feature/Admin/AdminSmokeTest.php`:

```php
test('admin puede navegar dashboard + sidebar links', function() {
    $admin = User::factory()->create()->assignRole('admin');
    $this->actingAs($admin);

    $routes = [
        'admin.dashboard', 'admin.news.index', 'admin.events.index',
        'admin.lodgings.index', 'admin.venues.index', 'admin.rentals.index',
        'admin.classifieds.index', 'admin.service-providers.index',
        'admin.recipes.index', 'admin.gallery.index', 'admin.tides.index',
        'admin.nearby-places.index', 'admin.useful-info.index', 'admin.pages.index',
        'admin.surveys.index', 'admin.newsletter-subscribers.index',
        'admin.newsletter-campaigns.index', 'admin.contact-messages.index',
        'admin.advertising-contacts.index', 'admin.users.index',
    ];

    foreach ($routes as $route) {
        $response = $this->get(route($route));
        expect($response->status())->toBe(200, "Route {$route} returned {$response->status()}");
    }
});
```

### Verificación final

```bash
docker compose exec -T app php artisan test
```

Esperado: todos verdes. Aproximadamente 120+ tests.

### Commit final

```bash
git add app/
git commit -m "feat(admin): tests E2E + matriz de roles + smoke test de navegación"
```

---

## Criterios de aceptación de Fase 4

- [ ] Admin layout funcional con sidebar responsive y flash messages.
- [ ] Middleware ForcePasswordReset redirige users con `must_reset_password=true`.
- [ ] 23 Policies implementadas y autodiscovered.
- [ ] CRUD completo para los 23 módulos.
- [ ] Media manager con upload + reorder + delete funcional.
- [ ] Role matrix correcta: admin=todo, editor=contenido, moderator=view+delete moderable.
- [ ] `php artisan test` verde (120+ tests totales).
- [ ] Login + dashboard + navegación manual sin errores visibles.

## Riesgos

1. **Autodiscovery de Policies** puede fallar si el namespace no es exacto — verificar con `php artisan model:show News` que muestre la policy.
2. **Media manager requiere `intervention/image`** — agregar a composer.json antes de Task 9.
3. **SortableJS via Vite** — asegurar que el bundle esté en `vite.config.js`.
4. **Tests que crean archivos** — usar `Storage::fake('public')` en setUp.
5. **Paginación con URL state** — si cliente quiere filtros persistentes en URL, Task 10 debe agregar `withQueryString()` a las paginaciones.
6. **Users con `must_reset_password`** no deberían poder usar API tokens — no aplica ahora (no hay API auth aún).

Con todo esto verde, se puede pasar al **Plan 5 — Sitio Público** (Fase 5 del spec).
